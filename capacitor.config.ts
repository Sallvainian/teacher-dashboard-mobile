import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.teacherdashboard.mobile',
  appName: 'academic-tools',
  webDir: 'build'
};

export default config;
